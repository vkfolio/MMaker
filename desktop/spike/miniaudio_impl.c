/* miniaudio's single translation unit. Kept alone so the 4 MB header is
   compiled exactly once and app code includes only the declarations. */
#define MINIAUDIO_IMPLEMENTATION
#include "miniaudio.h"

# Engine service. Model-specific deps (torch, demucs, diffusers, librosa,
# soundfile) are installed per-phase on the pod -- kept out of here so the
# service layer stays installable on a laptop for development against the stub.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pyyaml>=6.0
numpy>=1.26
requests>=2.32
huggingface-hub>=0.26
python-multipart>=0.0.9
